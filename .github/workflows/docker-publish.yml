# 工作流名称
name: Build and Push Docker Image to Docker Hub

# 触发条件：当向 main 分支 push 代码时
on:
  push:
    branches: ["main"]

# 工作任务
jobs:
  build-and-push:
    # 运行环境：使用GitHub提供的最新版Ubuntu服务器
    runs-on: ubuntu-latest

    steps:
      # 第一步：检出你的代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 第二步：登录到Docker Hub
      # 它会自动使用我们刚刚设置的DOCKERHUB_USERNAME和DOCKERHUB_TOKEN
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 第三步：构建并推送Docker镜像
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # 为镜像打上两个标签：一个固定版本号，一个latest最新版
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/font-manager:1.0, ${{ secrets.DOCKERHUB_USERNAME }}/font-manager:latest
